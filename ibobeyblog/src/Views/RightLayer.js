import React, { Component } from 'react';
import "../assets/_less/rightLayer.css";

export default class RightLayer extends Component {

    render() {
        return (
          <section className="rightLayer">
            RightLayer
          </section>
        );
      }
}
